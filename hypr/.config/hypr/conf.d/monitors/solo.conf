# Single-monitor layout — safe default on first boot, before you know your
# real output names. Once you do (`hyprctl monitors`), you can either edit
# this directly or add a named profile alongside it (see docked.conf) and
# switch between them live with `monitor-switch <name>`.
monitor=,preferred,auto,1
