# Auto-pick the highest refresh rate mode on whatever's connected, instead
# of naming outputs — handy on unfamiliar hardware or when you just don't
# care about exact placement. Hyprland resolves `highrr`/`highres` itself.
monitor=,highrr,auto,1
