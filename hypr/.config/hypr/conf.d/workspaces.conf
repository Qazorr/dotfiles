# Keep 1-5 alive even when empty, so they don't vanish from the bar.
workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
workspace = 4, persistent:true
workspace = 5, persistent:true

# Suggested fixed-purpose scheme (adjust to taste, or repurpose per the
# docked.conf monitor-binding example):
#   1 - terminal/editor    2 - browser/docs   3 - comms
#   4 - scratch            5+ - ad hoc
