# No rules here on purpose — Hyprland 0.55 removed matcher support from
# hyprlang `windowrule` (verified 0.55.2; see README.md's "Window rules"
# section for the exact errors). Rules that used to live here now sit inline
# on the exec dispatcher that launches each window instead — see
# conf.d/keybindings.conf. Windows we don't launch (Picture-in-Picture, VS
# Code's quick-open, pavucontrol) have no equivalent left; left unimplemented
# rather than kept as dead lines that look like they work.
